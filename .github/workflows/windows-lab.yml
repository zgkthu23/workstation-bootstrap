name: Build Windows app
on:
  workflow_dispatch:

jobs:
  explore-windows:
    runs-on: windows-latest
    steps:
      - name: Hello Windows
        run: |
          Write-Host "Hello from Windows runner"
          whoami
          Get-Location
          $PSVersionTable
